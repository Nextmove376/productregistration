// The brief specifies /api/enquiry; /api/contact is the original path and is
// already linked from the live site. Both share one implementation so neither
// breaks.
export { POST, runtime, dynamic } from '../contact/route';
